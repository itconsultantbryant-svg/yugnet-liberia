# Archived SQLite migrations

These migrations applied to the original local `file:./dev.db` SQLite database.
Production and current local development use **PostgreSQL**.

The live migration history is under `prisma/migrations/` (Postgres baseline).
Do not run these archived migrations against Postgres.
