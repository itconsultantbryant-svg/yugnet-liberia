import AdminPlaceholder from "@/components/ui/AdminPlaceholder";

export default function Page() {
  return (
    <AdminPlaceholder
      title="System Settings"
      blurb="Branding, notifications, integrations — later phase."
    />
  );
}
